plugins { id 'com.android.application' }

android {
    namespace 'cc.opentv.api21'
    compileSdk 35

    defaultConfig {
        applicationId 'cc.opentv.api21'
        minSdk 21
        targetSdk 35
        versionCode = (project.findProperty('ciVersionCode') ?: '1').toInteger()
        versionName = (project.findProperty('ciVersionName') ?: '1.0.0')
    }
    buildFeatures { viewBinding true; buildConfig true }

    def signingFile = System.getenv('OPENTV_KEYSTORE_FILE')
    def signingStorePassword = System.getenv('OPENTV_STORE_PASSWORD')
    def signingKeyAlias = System.getenv('OPENTV_KEY_ALIAS')
    def signingKeyPassword = System.getenv('OPENTV_KEY_PASSWORD')
    if (signingFile && signingStorePassword && signingKeyAlias && signingKeyPassword) {
        signingConfigs {
            release {
                storeFile file(signingFile)
                storePassword signingStorePassword
                keyAlias signingKeyAlias
                keyPassword signingKeyPassword
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            if (signingFile && signingStorePassword && signingKeyAlias && signingKeyPassword) {
                signingConfig signingConfigs.release
            }
        }
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'
    testImplementation 'junit:junit:4.13.2'
}
